# Validation documentation

Use the shared test plan and compatibility matrix to evaluate every candidate.
Save completed reports in `results/` with the name
`YYYY-MM-DD-<build>-<platform>.md`. Link that report from the candidate's build
manifest and record any failure before replacing the candidate.
