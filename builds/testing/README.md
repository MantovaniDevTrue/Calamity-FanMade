# Testing builds

Store release candidates and experiments in separate named directories. Each
candidate must include a build manifest and should link to its validation result
under `docs/validation/results/`.
