# Stable baselines

Only validated, known-good builds belong here. Treat every version directory as
immutable. If a correction is needed, create and validate a new version rather
than editing the old one.
