# Archive

The archive preserves important historical snapshots that are no longer the
active source or stable baseline. Archived content is immutable; append context
to its manifest rather than changing the snapshot itself.
