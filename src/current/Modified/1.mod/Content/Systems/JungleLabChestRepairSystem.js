import { ModSystem } from './../../TL/ModSystem.js';
import { WorldDB } from './../../TL/WorldDB.js';
import { EnsureAndFillJungleLabChest } from './../../Core/JungleLabRuntime.js';
const KEY='calamity:structure:jungleLab:',VERSION=1;
function I(v,f=-1){const n=Math.floor(Number(v));return Number.isFinite(n)?n:f;}
function Log(s){try{tl.log(`[CalamityPort JungleLabRepair] ${s}`);}catch(e){}}
export class JungleLabChestRepairSystem extends ModSystem{constructor(){super();this.Reset();}Reset(){this.Delay=240;this.Done=false;this.Attempts=0;}OnWorldLoad(){this.Reset();}OnWorldUnload(){this.Reset();}Update(){if(this.Done||!WorldDB.Instance||this.Delay-->0)return;if(WorldDB.get(KEY+'generated')!==true){this.Delay=300;return;}if(I(WorldDB.get(KEY+'lootVersion'),0)>=VERSION){this.Done=true;return;}const count=Math.max(0,I(WorldDB.get(KEY+'chestCount'),2));let ready=0,repaired=0;for(let i=0;i<count;i++){const x=I(WorldDB.get(KEY+`chest:${i}:x`),-1),y=I(WorldDB.get(KEY+`chest:${i}:y`),-1);if(x<0||y<0)continue;const r=EnsureAndFillJungleLabChest(x,y,i===0,true);if(r.success)ready++;if(I(r.filled,0)>0)repaired++;}this.Attempts++;Log(`verification ready=${ready}/${count}, repaired=${repaired}, attempt=${this.Attempts}.`);if(count>0&&ready===count){WorldDB.set(KEY+'lootVersion',VERSION);try{WorldDB.Instance.Save();}catch(e){}this.Done=true;}else if(this.Attempts>=8){Log('repair stopped after bounded retries.');this.Done=true;}else this.Delay=600;}}
