import { ModSystem } from './../../TL/ModSystem.js';

// Overloaded Sludge now uses one real recipe group instead of ten duplicate recipes.
export class OverloadedSludgeRecipeSystem extends ModSystem {}
