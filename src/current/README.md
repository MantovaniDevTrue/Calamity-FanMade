# Current source

This directory is the single active source tree for the TLPro/TLauncher port.
Place the incoming Calamity `.tl` files here and keep launcher-required paths
intact. Supporting source-owned assets may be placed in `assets/`.

Do not use this directory to store packaged builds. Candidates belong in
`builds/testing/`, and validated baselines belong in `builds/stable/`.
